import AppointmentClient from "./AppointmentClient";

export const metadata = {
  title: "Book Appointment | Neuro Clinic",
  description:
    "Book an appointment with Neuro Clinic for expert neurological consultation and advanced treatments.",
};

export default function AppointmentPage() {
  return <AppointmentClient />;
}
