import GalleryClient from "./GalleryClient";

export const metadata = {
  title: "Gallery | Mechnova Engineering",
  description:
    "Explore Mechnova Engineering gallery showcasing our projects, fabrication work, industrial structures, and completed installations.",
  keywords:
    "Mechnova gallery, engineering projects, fabrication work, industrial structures",
};

export default function GalleryPage() {
  return <GalleryClient />;
}
